services:

  frontend:
    build:
      context: .
      dockerfile: .docker/frontend.Dockerfile
    container_name: mvp-blogpost-app-frontend
    ports:
      - 3000:3000

  backend:
    build:
      context: .
      dockerfile: .docker/backend.Dockerfile
    container_name: mvp-blogpost-app-backend
    ports:
      - 8000:8000
    volumes:
      - ./backend:/mvp-blogpost-app
